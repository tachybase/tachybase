describe('hera-core > Plugin', () => {
  beforeEach(async () => {});

  afterEach(() => {});

  describe('create', () => {
    it('create with enabled', async () => {
      expect(10).toBe(10);
    });
  });
});
